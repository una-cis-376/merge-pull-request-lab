# merge-pull-request-lab
> cis 486 simple text file for merge &amp; pull request scenarios. 

## instructions

- fork this repository (creates a copy into your GitHub account)
- complete **Part A. GitHub pr/merge** & **Part B. Local .git**
- following the steps for each: creating branches, adding/editing files, & generating PR + merges.
- submit your forked repo link.

## Part A. GitHub pr/merge

### :one: `pr1` basic pr/merge, adding a new file, no conflict. 

- from the main branch, create a new branch named `pr1`
- on that branch, ...
- prune the branch

### 2️⃣ `pr2` basic pr/merge, editing an exisitng file, no conflict. 

- from the main branch, create a new branch named `pr2`
- on the `merge.md` file, add in a new line **underneath the existing text**
  - don't move existing lines...that'll create a conflict.
-  


🔹 Module 1 — Basic Pull Request (m1)
Goal: Understand GitHub UI PR workflow (no conflicts)
Students:
Work on branch m1
Edit m1.txt
Commit
Open PR → main
Merge via GitHub UI
Teaching point:
“PR is the conversation. Merge is the event.”
🔹 Module 2 — Merge (No Conflict) (m2)
Goal: Demonstrate clean merge**
Students branch off main
Edit merge.txt
Add lines that do NOT overlap
PR → m2
Merge (no conflicts)
🔹 Module 3 — Merge Conflict (m3)
Goal: Create + resolve conflict**
Students branch off m3
Edit same lines in merge.txt
PR → main
GitHub shows conflict
Resolve in UI
Merge
Teaching mantra:
“Stand on the branch that will receive the changes.”
